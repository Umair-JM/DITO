{
  "name": "dito-drop-in-the-ocean",
  "version": "1.0.0",
  "description": "DITO - Drop In The Ocean: an offline-first technical innovation evaluator and cosmic progress tracker.",
  "main": "app.js",
  "scripts": {
    "start": "npx -y http-server ./ -p 8080",
    "cap:init": "npx cap init DITO dito.ocean.app --web-dir=.",
    "cap:add-android": "npx cap add android",
    "cap:add-ios": "npx cap add ios",
    "cap:copy": "npx cap copy",
    "cap:sync": "npx cap sync",
    "cap:open-android": "npx cap open android"
  },
  "keywords": [
    "dito",
    "innovation-evaluator",
    "offline-first",
    "android",
    "sdk"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
